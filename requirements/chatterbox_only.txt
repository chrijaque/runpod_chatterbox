# ChatterboxTTS Only Requirements
# Minimal requirements for ChatterboxTTS without Higgs Audio conflicts

# RunPod SDK for handler functionality
runpod>=1.5.0

# Core dependencies
fastapi>=0.104.1
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6
pydantic>=2.5.0

# Firebase for cloud storage
firebase-admin>=6.2.0
google-cloud-storage>=2.10.0

# CORS and additional utilities
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4

# HTTP requests
requests>=2.31.0

# Audio processing
pydub==0.25.1
librosa>=0.10.0
soundfile>=0.12.0
numpy>=1.21.0
scipy>=1.9.0

# Text processing
nltk>=3.8.1

# Utilities
python-dotenv>=1.0.0
tqdm>=4.64.0
click>=8.0.0

# Note: PyTorch is already included in the base image
# Note: ChatterboxTTS will be installed from the forked repository 