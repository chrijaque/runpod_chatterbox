# Lightweight Requirements for Network Volume Deployment
# This file contains only the essential dependencies, excluding heavy models

# ===== CORE DEPENDENCIES =====

# RunPod SDK for handler functionality
runpod>=1.5.0

# FastAPI and web framework
fastapi>=0.104.1
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6
pydantic>=2.5.0

# Firebase for cloud storage
firebase-admin>=6.2.0
google-cloud-storage>=2.10.0

# CORS and additional utilities
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4

# HTTP requests
requests>=2.31.0

# ===== AUDIO PROCESSING =====

# Audio processing libraries (lightweight)
pydub==0.25.1
librosa>=0.10.0
soundfile>=0.12.0
numpy>=1.21.0
scipy>=1.9.0

# ===== TEXT PROCESSING =====

# NLTK for text processing
nltk>=3.8.1

# Higgs Audio specific text processing
langid>=1.1.6
jieba>=0.42.1

# ===== TRANSFORMERS & ML =====

# Transformers ecosystem (for Higgs Audio)
# Note: Higgs Audio requires specific transformers version
transformers>=4.45.1,<4.47.0
accelerate>=0.20.0
datasets>=2.10.0
tokenizers>=0.13.0

# HuggingFace Hub
huggingface-hub>=0.15.0

# ===== HIGGS AUDIO DEPENDENCIES =====

# Higgs Audio specific dependencies
einops>=0.6.0
sentencepiece>=0.1.99

# Higgs Audio specific dependencies from the forked repository
dacite
boto3==1.35.36
s3fs
json_repair
pandas
vector_quantize_pytorch
loguru
ruff==0.12.2
omegaconf

# ===== UTILITIES =====

# Utilities
tqdm>=4.64.0
click>=8.0.0
python-dotenv>=1.0.0

# Note: PyTorch is already included in the base image
# Note: descript-audio-codec will be installed separately to avoid protobuf conflicts
# Note: Models will be mounted from network volume at runtime 