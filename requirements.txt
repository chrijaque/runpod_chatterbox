# PyTorch ecosystem - installed by forked repository
# torch==2.6.0 (from forked repo)
# torchvision (from forked repo)  
# torchaudio==2.6.0 (from forked repo)

# Core dependencies - installed by forked repository
# transformers==4.46.3 (from forked repo)
# diffusers==0.29.0 (from forked repo)
# safetensors==0.5.3 (from forked repo)
# einops>=0.6.1 (from forked repo)

# Audio processing - installed by forked repository
# librosa==0.11.0 (from forked repo)
ffmpeg-python==0.2.0

# Voice cloning dependencies are included in the forked chatterbox repository
# conformer
# s3tokenizer  
# resemble-perth

# Utilities
huggingface_hub>=0.23.2
runpod>=1.7.7
aiohttp>=3.8.0
multidict>=6.0.0
yarl>=1.9.0
# Install forked repository with voice profile support
# Force install from git and prevent PyPI version
# NOTE: This is installed FIRST in Dockerfile to prevent dependency conflicts
# git+https://github.com/chrijaque/chatterbox_embed.git#egg=chatterbox-tts
